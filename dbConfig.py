dbConfig = {
    "user": "root",                # The MySQL username (root user has full privileges)
    "password": "--N0r0ffN1S",     # The password for the MySQL user
    "host": "127.0.0.1",           # The database host (localhost in this case)
    "database": "theoffice"         # The name of the database we are connecting to
}
